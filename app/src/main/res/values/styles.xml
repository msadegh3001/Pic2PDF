<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Application.Theme">
        <!-- Customize your theme here. -->
    </style>
    <style name="Application.Theme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!--    <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>-->
        <item name="fontFamily">@font/sans</item>
    </style>
    <style name="Application.Theme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="Application.Theme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
